# ats-protocol
ATS introduces a **"Living Knowledge Graph"** that sits inside your repository. It allows the AI to **Read**, **Instrument**, and **Silence** logs based on "Flows" (Features) rather than Files.
