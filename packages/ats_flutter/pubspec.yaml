name: ats_flutter
description: ATS Protocol Runtime SDK for Flutter — AI-driven flow-based telemetry. Provides ATS.trace() for structured, switchable debug logging with zero production overhead.
version: 0.3.0
homepage: https://github.com/nhanthuytech/ats-protocol
repository: https://github.com/nhanthuytech/ats-protocol/tree/main/packages/ats_flutter
issue_tracker: https://github.com/nhanthuytech/ats-protocol/issues
topics:
  - telemetry
  - debugging
  - logging
  - ai
  - developer-tools

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
