name: ats_mcp_server
description: ATS Protocol MCP Server — AI agent tools for flow graph management
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  analyzer: ^7.3.0
  path: ^1.9.0
